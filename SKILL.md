---
name: seedance-prompt-converter
description: "将 Seedance 2.0 风格的视频提示词诊断并重构为 Seedance 2.5 可执行的导演简报。用户要求 2.0 转 2.5、旧提示词适配、提示词迁移、排查 2.5 效果变差或优化 Seedance 2.5 提示词时使用。"
---

# Seedance Prompt Converter

把旧提示词的创作意图完整保留下来，消解互相竞争的指令，并输出可直接复制的 Seedance 2.5 提示词。不要把转换变成另写一个故事。

## 工作流

### 1. 收集输入

确认以下信息；能从原提示词推断时直接使用，缺失且会改变结果时用 `[需补充：...]` 标记，不擅自编造：

- 原始提示词
- 目标时长与画幅
- 参考图片、视频、音频及各自用途
- 必须保留的角色、产品、动作、台词和风格
- 是否需要对白、字幕、音效或音乐

### 2. 自动诊断

将提示词传给 `scripts/diagnose_prompt.py`：

```bash
python scripts/diagnose_prompt.py "<原提示词>"
python scripts/diagnose_prompt.py --file <prompt.txt>
```

脚本只负责发现常见风险，不代替语义判断。分镜中不同时间段使用不同运镜是合理的；不要把跨镜头差异误判为单镜头冲突。

### 3. 提取六个槽位

从原提示词提取：

1. 主体：谁或什么必须始终可辨认
2. 动作：可见的动作弧与结束状态
3. 场景：地点、时间、天气、空间关系和光源
4. 风格：一个主视觉方向及其可观察结果
5. 镜头：景别、机位、主运镜、速度和起止
6. 声音：对白、字幕、音效、环境声和音乐

### 4. 执行七项转换

转换时读取 [references/conversion-rules.md](references/conversion-rules.md)，重点完成：

1. 把空洞形容词和器材名改为可见结果
2. 消解风格、动作、镜头和场景冲突
3. 把长串负面词改成简短、可检查的约束
4. 按时间线拆分动作，每段保留一个核心变化
5. 用 `@image1`、`@video1`、`@audio1` 明确参考素材职责和排除项
6. 用 `(音乐/环境床)`、`<音效>`、`{对白}`、`【字幕】` 分离声音通道
7. 用 `[保持一致]` 锁定身份、服装、产品、道具归属和空间关系

需要对照写法时读取 [references/examples.md](references/examples.md)，只迁移方法，不复用示例主体、场景或台词。

### 5. 组装并自检

按“主体 + 动作/事件 + 场景与环境 + 视觉风格 + 镜头运动/切镜 + 声音”组装。

- 15 秒以内可用 3–5 个时间段；超过 15 秒必须有清晰时间线。
- 每个时间段只安排一个核心变化，并给出明确结束状态。
- 每个镜头只设一种主运镜；确需切换时写清顺序和切换点。
- 参考素材同时写“使用什么”和“不使用什么”。
- 精确文字、Logo 和复杂排版默认建议后期添加。
- 最后一段留出稳定收尾画面，不在动作中突然中断。

## 输出格式

依次输出：

1. `诊断结果`：按高、中、低优先级列出问题、证据和影响。
2. `转换后 Seedance 2.5 提示词`：放在一个可复制的 `plaintext` 代码块中。
3. `改动说明`：逐条说明改了什么、为什么改；必要时补充执行建议。

若原提示词已经符合 2.5 结构，明确说明无需重写，只做最小修正。
